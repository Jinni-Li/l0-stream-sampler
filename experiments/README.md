# Experiments

This directory holds reproducible experiment definitions. Store small,
versioned configuration files in `configs/` and document the command, random
seed, input provenance, and expected outputs for each experiment.

Generated datasets and results belong in `data/processed/` and `results/`, not
in this directory.

