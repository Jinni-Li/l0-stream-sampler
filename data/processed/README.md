# Processed data

Datasets produced from raw inputs are placed here, including cleaned streams and LOBSTER-to-turnstile conversions. Files in this directory are generated or may be large, so Git ignores everything except this README.

The code or configuration used to recreate processed data are kept under `scripts/` or `experiments/`.

